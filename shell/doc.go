// Package shell contains functions related to running shell commands. The
// generation of the command syntax is separated from the execution of the syntax
// to improve testing. We just verify that we're creating the command syntax
// correctly and assume that the shell and commands will work.
package shell
